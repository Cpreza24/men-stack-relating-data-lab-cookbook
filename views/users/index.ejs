<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>
    <body>
        <%- include('../partials/_navbar') %>
        <h1>All Users</h1>
        <ul>
            <% users.forEach(user => { %>
                <a href="/all_users/<%= user._id %>">
                    <li><%= user.username %></li>
                </a>
            <% }) %>
        </ul>
    </body>
</html>